(()=>{'use strict';const page=document.querySelector('.service-index');if(!page)return;const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const items=[...page.querySelectorAll('.si-intro>div>*,.si-band>div,.si-feature>div>*,.si-fleet>div')];items.forEach(el=>el.classList.add('si-reveal'));if(reduce||!('IntersectionObserver'in window))items.forEach(el=>el.classList.add('visible'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.13,rootMargin:'0px 0px -6%'});items.forEach((el,i)=>{el.style.transitionDelay=`${(i%2)*90}ms`;io.observe(el)})}if(!reduce){const hero=page.querySelector('.si-hero>img');let raf=0;const move=()=>{raf=0;if(hero)hero.style.transform=`translate3d(0,${Math.min(scrollY*.065,55)}px,0) scale(1.05)`};addEventListener('scroll',()=>{if(!raf)raf=requestAnimationFrame(move)},{passive:true});move()}})();
