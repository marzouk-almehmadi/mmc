(()=>{'use strict';const root=document.querySelector('.editorial main');if(!root)return;const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const items=[...root.querySelectorAll('[data-ed-reveal]')];if(reduce||!('IntersectionObserver'in window))items.forEach(x=>x.classList.add('visible'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -6%'});items.forEach((x,i)=>{x.style.transitionDelay=`${(i%3)*70}ms`;io.observe(x)})}if(!reduce){const hero=root.querySelector('.ed-hero>img');let raf=0;const update=()=>{raf=0;if(!hero)return;hero.style.transform=`translate3d(0,${Math.min(scrollY*.07,60)}px,0) scale(1.05)`};addEventListener('scroll',()=>{if(!raf)raf=requestAnimationFrame(update)},{passive:true});update()}})();
